# Top League Tech — Astro static migration

Built from the supplied WebArchive snapshots. The project is intentionally static: no `@astrojs/cloudflare` adapter, no SSR, no Wrangler configuration.

Cloudflare Pages:
- Root directory: `/`
- Build command: `npm run build`
- Output directory: `dist`
